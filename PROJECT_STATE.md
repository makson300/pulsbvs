# Пульс БВС — память проекта

Этот файл нужен, чтобы не терять контекст при сжатии истории, смене модели или перезапуске Harvi Code.

Для старта нового чата также подготовлен файл `NEXT_CHAT_BRIEF.md` — в нём есть готовый стартовый промпт, фактическое состояние и следующий безопасный инкремент.

## 1. Ключевая цель продукта

**Пульс БВС** — веб-продукт для контроля флота БВС/дронов и предиктивной диагностики, особенно батарей и эксплуатационных рисков.

Ориентир по ценности: AirData-подобный продукт, но адаптированный под российский рынок, локальную эксплуатацию и реальные ограничения доступа к зарубежным сервисам/инструментам.

Первый фокус: предиктивные алерты по дронам и батареям. Текущий реальный парк для получения первых журналов: **DJI Avata 2, DJI Avata 360 и DJI Mini 4 Pro**. DJI Agras T40 остаётся отдельным будущим профилем до появления его журналов.

## 2. Жёсткие пользовательские требования

- Всегда отвечать пользователю на русском языке.
- Telegram не использовать: пользователь указал, что он заблокирован/неподходящ для РФ-контекста.
- UI должен быть современным, тёмным, сине-сапфировым.
- Не делать только статичный макет: кнопки, страницы и сценарии должны работать.
- Закрывать цельными проверенными этапами/пунктами, а не дробить работу на бесконечные микрошаги.
- Выбирать решения так, чтобы потом не пришлось полностью переделывать архитектуру.
- Важную информацию перепроверять, не выдумывать факты и не обещать невозможное.
- DAT/ZIP не считать поддержанными для реальной аналитики/истории до подтверждённого декодера и схемы.
- Не вставлять токены/секреты в чат, код, конфиги или документацию.

## 3. Текущий стек

- Vite
- React
- TypeScript
- Vitest
- `lucide-react`
- Демо-хранилище: `localStorage`
- Backend, настоящая авторизация и база данных пока не реализованы.

Основные команды:

```bash
npm run dev -- --host 127.0.0.1
npm test
npm run build
npm audit --omit=optional
```

## 4. Текущая структура проекта

Ключевые файлы:

- `src/App.tsx` — основной UI; файл всё ещё один, но после refactor pass разделён внутри на именованные компоненты (`Sidebar`, `Topbar`, `Overview`, `FleetView`, `BatteriesView`, `FlightsView`, `ImportHistory`, `UploadModal` и др.).
- `src/styles.css` — стили тёмно-сапфирового интерфейса; sidebar footer закреплён и проверен после регрессии.
- `src/analytics/telemetry.ts` — парсинг и анализ телеметрии.
- `src/analytics/batteryRisk.ts` — правила оценки батарейных рисков.
- `src/analytics/telemetry.test.ts` — тесты парсинга CSV/KML, DAT/ZIP и профиля импорта.
- `src/analytics/batteryRisk.test.ts` — тесты батарейных правил.
- `src/analytics/demoLogs.ts` — демо-сценарии телеметрии.
- `src/domain/fleet.ts` — доменные типы активов и localStorage-хранилище истории импортов.
- `src/domain/fleet.test.ts` — тесты доменной модели активов и истории.
- `.harvi/skills/**` — проектные Harvi skills, теперь отслеживаются Git; остальные `.harvi/*` остаются ignored.
- `README.md` — краткое описание проекта.
- `NEXT_CHAT_BRIEF.md` — инструкция для продолжения разработки в новом чате.
- `PRODUCT_ANALYSIS.md` — сравнение с AirData, границы MVP и актуальная дорожная карта.
- `docs/BACKEND_API_CONTRACT_V1.md` — проектный API-контракт будущего backend v1.
- `docs/adr/0001-node-postgresql-backend.md` — ADR единого Node.js backend и PostgreSQL для пилота.
- `docs/DJI_AGRAS_T40_ANONYMIZED_LOG_REQUIREMENTS.md` — требования к безопасным обезличенным T40-журналам и фикстурам.
- `docs/ИНСТРУКЦИЯ_ПО_ПОЛУЧЕНИЮ_ЖУРНАЛОВ_DJI.md` — путь получения и обезличивания журналов Avata 2, Avata 360 и Mini 4 Pro.

Важно: `src/App.tsx` был структурно приведён в порядок внутри одного файла, но ещё не разнесён по отдельным файлам. Следующее разделение компонентов стоит делать только вместе с реальной необходимостью, не как самостоятельную большую перестройку.

## 5. Что уже реализовано

### Публичная часть

- Главная landing page.
- Описание ценности продукта.
- Блоки преимуществ/болей.
- CTA для входа в демо и пилотного доступа.
- Тарифная модель:
  - **Старт** — 4 900 ₽, до 3 дронов, 60 анализов/мес.
  - **Флот** — 14 900 ₽, до 15 дронов, 400 анализов/мес.
  - **Enterprise** — по договору, 50+ дронов, SLA/on-prem/API.

### Демо-аккаунт

- Демо-регистрация/вход через `localStorage`.
- Ключ локального хранилища: `puls-bvs-user`.
- Переключение landing ↔ dashboard.
- Загрузка профиля пользователя сделана безопаснее после refactor pass.

### Dashboard

Рабочие разделы левого меню:

- Обзор
- Флот
- Батареи
- Полёты
- Техническое обслуживание

Есть рабочие кнопки:

- загрузка файла;
- тестовые сценарии;
- рекомендации;
- уведомления;
- настройки;
- помощь;
- возврат на главную.

Sidebar footer regression исправлена: пункты `Главная`, `Настройки`, `Помощь` видимы снизу sidebar; добавлен `data-testid="sidebar-footer"`.

### Активы и история импортов

- Демо-дроны и демо-батареи вынесены в доменную модель `src/domain/fleet.ts`. Новая пустая установка содержит Avata 2, Avata 360 и Mini 4 Pro; для них до журнала не показываются здоровье, налёт или циклы. Существующие данные `localStorage` не изменяются автоматически.
- В разделах «Флот» и «Батареи» можно добавлять новые демо-активы; они сохраняются в `localStorage`.
- Повторный ввод существующего ID батареи выбирает существующую батарею вместо создания дубля.
- Ключ локального хранилища активов и истории: `puls-bvs-fleet-state`.
- При импорте пользователь выбирает дрон и батарею.
- Поддерживаемые CSV/TXT/KML импорты сохраняются в историю с именем файла, временем, активами, профилем, качеством, алертами и исходным результатом анализа.
- Сохранённый анализ можно открыть из «Истории импортов» после перезагрузки страницы; у блока есть `data-testid="import-history"`.
- DAT/ZIP не создают полноценную запись истории, пока нет подтверждённого декодера.

### Импорт и аналитика

Поддерживается:

- CSV/TXT как табличная телеметрия;
- KML как маршрутный источник;
- DAT/ZIP принимаются как неподдержанные источники с честным сообщением: декодер ещё не подключён, аналитика не выполняется.

Аналитика умеет:

- определять доступные/отсутствующие поля;
- считать Data Quality Score;
- классифицировать источник импорта:
  - `csv`,
  - `kml`,
  - `unsupported` для DAT/ZIP,
  - `unknown`;
- показывать паспорт импорта:
  - маршрутный источник;
  - базовая батарейная телеметрия;
  - расширенная батарейная телеметрия;
- рекомендовать следующий лучший файл для более точной диагностики;
- давать пользователю скачиваемые примеры CSV и KML.

### Батарейные риски

Реализованы первые консервативные правила:

- ограниченные данные (`BATTERY_DATA_LIMITED`);
- разбаланс ячеек (`BATTERY_CELL_IMBALANCE`);
- перегрев батареи (`BATTERY_OVERHEAT`);
- критическое низкое напряжение (`BATTERY_UNDERVOLTAGE`);
- аномальная просадка напряжения (`BATTERY_VOLTAGE_SAG`);
- ускоренный разряд (`BATTERY_RAPID_DRAIN`);
- ошибка батареи в логе (`BATTERY_DEVICE_ERROR`).

Важно: эти правила — первичный triage, не замена официальной диагностике или инструкции DJI. `BATTERY_DEVICE_ERROR` не надо подключать к реальным CSV warning-строкам до подтверждённой схемы журнала.

### Тестовая защита

Подключён `vitest@3.2.6`.

Покрыты тестами:

- route-only данные не обещают оценку батареи;
- T40 undervoltage ниже 47.6 В даёт critical alert;
- сильный разбаланс ячеек даёт critical alert;
- перегрев батареи даёт critical alert;
- CSV с ячейками определяется как расширенная батарейная телеметрия;
- KML парсится как маршрутный источник;
- CSV без ячеек определяется как базовая батарейная телеметрия;
- пустые и битые CSV/KML не создают фальшивых точек и батарейных выводов;
- DAT/ZIP не запускают фальшивую аналитику до появления отдельного декодера;
- доменная история импортов сохраняет поддерживаемый анализ и не создаёт полноценные записи для DAT/ZIP.

## 6. Проверенные факты по DJI Agras T40

- KML/SmartFarm полезны для маршрутов, скорости, зоны работ и активности, но часто недостаточны для глубокой батарейной диагностики.
- Для серьёзной предиктивной аналитики T40 нужны расширенные логи с дрона/пульта/обслуживания, желательно с напряжениями ячеек, температурой, предупреждениями, нагрузкой и историей батареи.
- Критическое напряжение пакета T40: **47.6 В** — ниже этого порога возможны сценарии автоматического снижения/посадки.
- Рискованные условия батареи: температура выше 50 °C, повреждения, вздутие, падение, вода/влага.
- Осмотр батареи важен каждые 100 полётов / 20 часов эксплуатации.
- Проблемы двигателя могут проявляться через шум, сопротивление, коррозию, подшипники и неровную работу.

## 7. MCP / Harvi Code

Пользователь работает в VS Code через Harvi Code.

Место MCP-конфига Harvi Code:

```text
C:/Users/Mvoro/AppData/Roaming/Code/User/globalStorage/kristianshin.harvi-code/settings/mcp_settings.json
```

Подключены MCP-серверы:

- `filesystem-puls-bvs` — доступ к текущему проекту.
- `puppeteer` — для проверки интерфейса в браузере.
- `sequential-thinking` — для сложного пошагового анализа.
- `github` — подключён через переменную окружения `GITHUB_PERSONAL_ACCESS_TOKEN`.

Не подключены пока:

- Brave Search MCP — нужен API-ключ.
- Postgres MCP — будет нужен после появления backend/БД.

Проектные Harvi skills находятся в `.harvi/skills/**` и отслеживаются Git. В текущем репозитории есть skills для adapter/backend contracts, architecture review, browser smoke, context memory, docs, frontend, import pipeline, product, real logs, release, security/privacy, storage, tests/UI regression и roadmap/safety. В новом чате Harvi должен перечитать их из Git; если часть skills не отображается сразу, сначала проверить файлы на диске.

## 8. GitHub token policy

Не вставлять токены напрямую в чат, код, документацию или MCP-конфиг.

Правильный путь на Windows:

```cmd
setx GITHUB_PERSONAL_ACCESS_TOKEN "НОВЫЙ_ТОКЕН"
```

После этого перезапустить VS Code/Harvi Code и подключать GitHub MCP через env-переменную.

Важно: пользователь случайно отправил GitHub token в чат. Этот токен нужно считать скомпрометированным и не использовать. Пользователю рекомендовано отозвать его и создать новый fine-grained token.

Текущий GitHub-репозиторий:

```text
https://github.com/makson300/pulsbvs
```

Локальная ветка `main` отслеживает `origin/main`.

Последние подтверждённые коммиты перед этим memory-pass:

```text
735690b Track project Harvi skills
9e76312 Refactor dashboard app structure
bc4a4db Keep sidebar footer navigation visible
e9fa66e Add fleet assets and import history
3f7fb2f Restore sidebar footer navigation
9de86a2 Revise product roadmap from AirData analysis
47acd21 Handle unsupported DAT and ZIP imports
7ae79fb Harden telemetry parsing for malformed inputs
```

## 9. Текущее состояние проверки

Последние функциональные проверки до memory-pass проходили успешно:

```bash
npm test
npm run build
npm audit --omit=optional
```

Последний известный результат:

```text
Tests: 15 passed
Build: ✓ built
Audit: found 0 vulnerabilities
```

Puppeteer/browser smoke ранее подтвердил:

- Stage 2 flow: импорт с выбранными `Agras T40 №02` + `BT-009`, история создаётся и сохранённый анализ открывается после навигации.
- Добавление демо-дрона/батареи через UI работает.
- Sidebar footer после фикса видим: `Главная`, `Настройки`, `Помощь`.
- После refactor pass чистый smoke подтвердил footer, историю импортов и связку активов.

Dev server в момент memory-pass был активен как managed process `82`:

```text
npm run dev -- --host 127.0.0.1
http://127.0.0.1:5173/
```

## 10. Что делать, если агент зациклился или потерял контекст

Если модель начинает повторяться, забывать решения или менять не те файлы, пользователь может написать:

```text
Стоп. Ты зациклился. Перечитай PROJECT_STATE.md, NEXT_CHAT_BRIEF.md, README.md, package.json и продолжай с последнего проверенного шага.
```

После этого агент должен:

1. остановить изменения наугад;
2. перечитать `PROJECT_STATE.md`, `NEXT_CHAT_BRIEF.md`, `README.md`, `package.json` и релевантные файлы `src`;
3. проверить фактическое состояние проекта через `git status --short --branch` и при необходимости `git log --oneline -8`;
4. выполнить релевантные проверки (`npm test`, `npm run build`, `npm audit --omit=optional`) после изменений;
5. продолжать только от подтверждённого состояния;
6. не начинать Stage 3 implementation-first без API contracts/ADR и требований к реальным логам.

## 11. Актуальная дорожная карта

Подробное обоснование и границы MVP — в `PRODUCT_ANALYSIS.md`. AirData используется как ориентир операционного контура «импорт → актив → риск → ТО → уведомление → отчёт», а не как перечень экранов для копирования.

1. **Основание пилота: активы и импорт — завершено в frontend-демо.** Реализованы доменные типы, реестр текущего парка без ложных показателей до журнала, выбор дрона/батареи, история поддерживаемых импортов в `localStorage`, открытие сохранённого анализа, sidebar regression fix и refactor структуры `App.tsx`.
2. **Подготовка backend-контура — завершена документально.** Добавлены [API-контракт v1](docs/BACKEND_API_CONTRACT_V1.md) и [ADR 0001](docs/adr/0001-node-postgresql-backend.md) для единого Node backend, PostgreSQL, ORM/миграций, оригиналов логов, аудита и on-prem. Backend-код, БД и аутентификация не реализованы.
3. **Достоверный адаптер первой подтверждённой DJI-модели.** Получить обезличенный реальный журнал Avata 2, Avata 360 или Mini 4 Pro по [инструкции](docs/ИНСТРУКЦИЯ_ПО_ПОЛУЧЕНИЮ_ЖУРНАЛОВ_DJI.md), зафиксировать схему, написать адаптер, профиль порогов и контрактные тесты. T40 отложен до появления его данных. DAT/ZIP декодировать только после отдельного исследования формата.
4. **История батареи и ТО.** Накопить сравнимые полёты, показывать тенденции только при достаточной выборке, создавать и закрывать объяснимые задачи обслуживания.
5. **Операционная готовность.** Добавить карту/временную шкалу для доступной телеметрии, отчёты HTML/PDF/CSV, аудит и email/webhook-уведомления. Telegram не использовать.
6. **Масштабирование по спросу.** Мультиорганизационность, API, площадки, документы, чек-листы, дополнительные модели БВС и только затем дорогие функции наподобие streaming/планирования.

Первый следующий инкремент для нового чата: **не начинать backend-код или адаптер наугад**. API-контракт и ADR уже подготовлены; получить разрешённый обезличенный журнал одной модели текущего парка по инструкции, подтвердить схему и только затем планировать адаптер.

## 12. Принцип разработки

- Закрывать цельные этапы/пункты с проверкой, а не возвращаться к пользователю после каждого мелкого шага.
- После каждого значимого изменения запускать `npm run build`.
- Для аналитики запускать `npm test` перед commit/push.
- Перед push по возможности запускать `npm audit --omit=optional`.
- Не симулировать аналитику там, где данных недостаточно.
- Честно показывать пользователю ограничения загруженного файла.
- Не использовать Telegram.
- Не внедрять тяжёлую архитектуру раньше необходимости.
- Документы должны различать: что сделано, что проверено, что закоммичено/запушено, и что только планируется.
