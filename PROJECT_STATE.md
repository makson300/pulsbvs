# Пульс БВС — память проекта

Этот файл нужен, чтобы не терять контекст при сжатии истории, смене модели или перезапуске Harvi Code.

## 1. Ключевая цель продукта

**Пульс БВС** — веб-продукт для контроля флота БВС/дронов и предиктивной диагностики, особенно батарей и эксплуатационных рисков.

Ориентир по ценности: Airdata-подобный продукт, но адаптированный под российский рынок, локальную эксплуатацию и реальные ограничения доступа к зарубежным сервисам/инструментам.

Первый фокус: предиктивные алерты по дронам и батареям. Важный целевой аппарат на следующих этапах — **DJI Agras T40**.

## 2. Жёсткие пользовательские требования

- Всегда отвечать пользователю на русском языке.
- Telegram не использовать: пользователь указал, что он заблокирован/неподходящ для РФ-контекста.
- UI должен быть современным, тёмным, сине-сапфировым.
- Не делать только статичный макет: кнопки, страницы и сценарии должны работать.
- Выбирать решения так, чтобы потом не пришлось полностью переделывать архитектуру.
- Важную информацию перепроверять, не выдумывать факты и не обещать невозможное.

## 3. Текущий стек

- Vite
- React
- TypeScript
- `lucide-react`
- Демо-хранилище: `localStorage`
- Backend, настоящая авторизация и база данных пока не реализованы.

Основные команды:

```bash
npm run dev -- --host 127.0.0.1
npm run build
```

## 4. Текущая структура проекта

Ключевые файлы:

- `src/App.tsx` — основной UI, пока компоненты в одном файле.
- `src/styles.css` — стили тёмно-сапфирового интерфейса.
- `src/analytics/telemetry.ts` — парсинг и анализ телеметрии.
- `src/analytics/batteryRisk.ts` — правила оценки батарейных рисков.
- `src/analytics/demoLogs.ts` — демо-сценарии телеметрии.
- `README.md` — описание проекта.

Важно: `src/App.tsx` уже стал достаточно крупным. Скоро желательно вынести компоненты по файлам, но не делать это раньше, чем появится практическая необходимость.

## 5. Что уже реализовано

### Публичная часть

- Главная landing page.
- Описание ценности продукта.
- Блоки преимуществ/болей.
- CTA для входа в демо и пилотного доступа.
- Тарифная модель:
  - **Старт** — 4 900 ₽, до 3 дронов, 60 анализов/мес.
  - **Флот** — 14 900 ₽, до 15 дронов, 400 анализов/мес.
  - **Enterprise** — по договору, 50+ дронов, SLA/on-prem/API.

### Демо-аккаунт

- Демо-регистрация/вход через `localStorage`.
- Ключ локального хранилища: `puls-bvs-user`.
- Переключение landing ↔ dashboard.

### Dashboard

Рабочие разделы левого меню:

- Обзор
- Флот
- Батареи
- Полёты
- Техническое обслуживание

Есть рабочие кнопки:

- загрузка файла;
- тестовые сценарии;
- рекомендации;
- уведомления;
- настройки;
- помощь;
- возврат на главную.

### Импорт и аналитика

Поддерживается:

- CSV/TXT как табличная телеметрия;
- KML как маршрутный источник;
- DAT/ZIP пока принимаются в интерфейсе как будущие источники, но без фальшивого анализа.

Аналитика умеет:

- определять доступные/отсутствующие поля;
- считать Data Quality Score;
- классифицировать источник импорта:
  - `csv`,
  - `kml`,
  - `unknown`;
- показывать паспорт импорта:
  - маршрутный источник;
  - базовая батарейная телеметрия;
  - расширенная батарейная телеметрия;
- рекомендовать следующий лучший файл для более точной диагностики.

### Батарейные риски

Реализованы первые консервативные правила:

- ограниченные данные (`BATTERY_DATA_LIMITED`);
- разбаланс ячеек (`BATTERY_CELL_IMBALANCE`);
- перегрев батареи (`BATTERY_OVERHEAT`);
- критическое низкое напряжение (`BATTERY_UNDERVOLTAGE`);
- аномальная просадка напряжения (`BATTERY_VOLTAGE_SAG`);
- ускоренный разряд (`BATTERY_RAPID_DRAIN`);
- ошибка батареи в логе (`BATTERY_DEVICE_ERROR`).

Важно: эти правила — первичный triage, не замена официальной диагностике или инструкции DJI.

## 6. Проверенные факты по DJI Agras T40

- KML/SmartFarm полезны для маршрутов, скорости, зоны работ и активности, но часто недостаточны для глубокой батарейной диагностики.
- Для серьёзной предиктивной аналитики T40 нужны расширенные логи с дрона/пульта/обслуживания, желательно с напряжениями ячеек, температурой, предупреждениями, нагрузкой и историей батареи.
- Критическое напряжение пакета T40: **47.6 В** — ниже этого порога возможны сценарии автоматического снижения/посадки.
- Рискованные условия батареи: температура выше 50 °C, повреждения, вздутие, падение, вода/влага.
- Осмотр батареи важен каждые 100 полётов / 20 часов эксплуатации.
- Проблемы двигателя могут проявляться через шум, сопротивление, коррозию, подшипники и неровную работу.

## 7. MCP / Harvi Code

Пользователь работает в VS Code через Harvi Code.

Место MCP-конфига Harvi Code:

```text
C:/Users/Mvoro/AppData/Roaming/Code/User/globalStorage/kristianshin.harvi-code/settings/mcp_settings.json
```

Подключены MCP-серверы:

- `filesystem-puls-bvs` — доступ к текущему проекту.
- `puppeteer` — для будущей проверки интерфейса в браузере.
- `sequential-thinking` — для сложного пошагового анализа.

Не подключены пока:

- GitHub MCP — нужен новый безопасный токен через переменную окружения.
- Brave Search MCP — нужен API-ключ.
- Postgres MCP — будет нужен после появления backend/БД.

Важно: пользователь случайно отправил GitHub token в чат. Этот токен нужно считать скомпрометированным и не использовать. Пользователю рекомендовано отозвать его и создать новый fine-grained token.

## 8. GitHub token policy

Не вставлять токены напрямую в чат, код или MCP-конфиг.

Правильный путь на Windows:

```cmd
setx GITHUB_PERSONAL_ACCESS_TOKEN "НОВЫЙ_ТОКЕН"
```

После этого перезапустить VS Code/Harvi Code и подключать GitHub MCP через env-переменную.

## 9. Текущее состояние проверки

Последние сборки `npm run build` проходили успешно.

Последний известный успешный результат после добавления паспорта импорта:

```text
✓ built in 1.48s
```

Dev server был запущен:

```text
http://127.0.0.1:5173/
```

## 10. Что делать, если агент зациклился или потерял контекст

Если модель начинает повторяться, забывать решения или менять не те файлы, пользователь может написать:

```text
Стоп. Ты зациклился. Перечитай PROJECT_STATE.md, README.md, package.json и продолжай с последнего проверенного шага.
```

После этого агент должен:

1. остановить изменения наугад;
2. перечитать `PROJECT_STATE.md`, `README.md`, `package.json` и релевантные файлы `src`;
3. проверить фактическое состояние проекта;
4. выполнить `npm run build` после изменений;
5. продолжать только от подтверждённого состояния.

## 11. Ближайшие разумные следующие шаги

1. Улучшить страницу результата импорта:
   - показать список распознанных колонок;
   - показать отсутствующие критичные поля;
   - дать подсказки по названиям колонок для CSV/TXT.
2. Добавить локальные тесты для `telemetry.ts` и `batteryRisk.ts`.
3. Подготовить структуру backend без привязки к Telegram:
   - организации;
   - пользователи;
   - дроны;
   - батареи;
   - полёты;
   - анализы;
   - задачи обслуживания.
4. Позже подключить БД и настоящую авторизацию.
5. Позже добавить GitHub MCP только через безопасную env-переменную.

## 12. Принцип разработки

- Делать небольшие работающие шаги.
- После каждого значимого изменения запускать `npm run build`.
- Не симулировать аналитику там, где данных недостаточно.
- Честно показывать пользователю ограничения загруженного файла.
- Не использовать Telegram.
- Не внедрять тяжёлую архитектуру раньше необходимости.
